# Firebase Monitoring Environment Variables (v2)
# このファイルをコピーして .env を作成し、適切な値を設定してください
# cp .env.example .env

# 環境名 (dev/prod)
ENVIRONMENT=dev

# Slack Webhook URLs - チャンネル別設定
# 取得方法: https://api.slack.com/messaging/webhooks
#
# 推奨チャンネル構成:
#   Dev環境:
#     - #firebase-dev-reports  : 定時レポート（日次/週次）
#     - #firebase-dev-alerts   : アラート・エラー通知
#   Prod環境:
#     - #firebase-prod-reports : 定時レポート（日次/週次）
#     - #firebase-prod-alerts  : アラート・エラー通知

# Dev環境用Webhook URLs
SLACK_DEV_REPORTS_URL=https://hooks.slack.com/services/YOUR_WORKSPACE_ID/YOUR_CHANNEL_ID/YOUR_TOKEN
SLACK_DEV_ALERTS_URL=https://hooks.slack.com/services/YOUR_WORKSPACE_ID/YOUR_CHANNEL_ID/YOUR_TOKEN

# Prod環境用Webhook URLs
SLACK_PROD_REPORTS_URL=https://hooks.slack.com/services/YOUR_WORKSPACE_ID/YOUR_CHANNEL_ID/YOUR_TOKEN
SLACK_PROD_ALERTS_URL=https://hooks.slack.com/services/YOUR_WORKSPACE_ID/YOUR_CHANNEL_ID/YOUR_TOKEN

# Firebase Project ID (自動的に設定されるため通常は不要)
# FIREBASE_PROJECT_ID=chat-sns-project
